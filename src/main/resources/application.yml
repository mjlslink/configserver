spring:
  application:
    name: "configserver"
  profiles:
#    active: native
    active: git
  cloud:
    config:
      server:
# File system based configuration
#        native:
#          search-locations: "classpath:/config"
        git:
          uri: "https://github.com/mjlslink/bankapp-config.git"
          default-label: master
          timeout: 5
          clone-on-start: true
          force-pull: true
encrypt:
  key: "my-encryption-key-is-symmetric"

server:
  port:
    8071